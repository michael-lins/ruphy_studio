<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Simulated Borderless Window</title>
  <style>
    body {
      margin: 0;
      font-family: Arial, sans-serif;
    }
    #modal {
      position: fixed;
      top: 0;
      left: 0;
      width: 100vw;
      height: 100vh;
      background-color: #ffffff;
      box-shadow: 0 0 20px rgba(0, 0, 0, 0.5);
      z-index: 9999;
      display: none; /* Hide initially */
    }
    #modal-header {
      padding: 10px;
      background-color: #333;
      color: white;
      font-size: 16px;
    }
    #modal-content {
      padding: 20px;
    }
    #close-modal {
      float: right;
      cursor: pointer;
      background: none;
      border: none;
      color: white;
      font-size: 16px;
    }
  </style>
</head>
<body>
  <button onclick="showModal()">Open Modal</button>

  <div id="modal">
    <div id="modal-header">
      <button id="close-modal" onclick="closeModal()">✖</button>
      Modal Header
    </div>
    <div id="modal-content">
      <h1>Simulated Window</h1>
      <p>This is a full-screen modal acting as a borderless window!</p>
    </div>
  </div>

  <script>
    function showModal() {
      document.getElementById('modal').style.display = 'block';
    }
    function closeModal() {
      document.getElementById('modal').style.display = 'none';
    }
  </script>
</body>
</html>
